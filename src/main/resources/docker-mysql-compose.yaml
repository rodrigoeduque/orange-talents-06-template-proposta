version: "3"

services:
  db-mysql:
    image: mysql:latest
    volumes:
      - db_data:/var/lib/mysql
      ports:
        - 3306:3306
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_USER: root
volumes:
  db_data: {}
